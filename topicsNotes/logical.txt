you can use and condition with && operator

if [[ condition1 ]] && [[ condition2 ]] / if [[ condition1 && condition2 ]]
                        // both syntax are correct
then
	some task
else
	some other task
fi

you can give different conditions in different double paranthesis or in single paranthesis both are acceptable

you can use or condition using || operator
