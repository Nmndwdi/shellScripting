#!/bin/bash

# RANDOM -> to generate a random number between 0 and 32767

echo "Random number generated is $RANDOM"

# UID -> to get the user ID of the logged in user

echo "User ID  of the logged in user i.e. $USER is $UID"

# USER -> to get the current user
